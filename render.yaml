services:
  - type: web
    name: velodesk-signaling
    plan: free
    runtime: node
    rootDir: signaling-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
